# latihan1.py

from random import random

n = int(input("Masukkan nilai N: "))
i = 0

while i < n:
    for _ in range(1):  # Kombinasi dengan for, meskipun hanya satu iterasi
        angka = random()
        if angka < 0.5:
            print(f"data ke: {i + 1} = {angka}")
            i += 1